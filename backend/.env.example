# Pimlico (ERC-4337 bundler/paymaster)
PIMLICO_API_KEY=

# RPC URLs
RPC_URL_ETHEREUM=
RPC_URL_BASE=
RPC_URL_POLYGON=
RPC_URL_ARC=

# Relayer private key (EOA that signs session keys / fallback txs)
PRIVATE_KEY_RELAYER=

# USDC contract addresses per chain
USDC_ADDRESS_BASE=
USDC_ADDRESS_POLYGON=
USDC_ADDRESS_ETHEREUM=

# CCTP TokenMessenger contract addresses per chain
CCTP_CONTRACT_BASE=
CCTP_CONTRACT_POLYGON=

# Session key TTL in seconds (e.g. 86400 = 24h)
SESSION_KEY_EXPIRY=86400
